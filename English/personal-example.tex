%!TEX encoding = UTF8
%!TEX root = cv-llt.tex
%
% Template for private contact details.
%
% Copy this file to personal.tex and fill in your real values:
%
%     cp personal-example.tex personal.tex
%
% personal.tex is gitignored, so a phone number or WeChat ID never lands in a
% commit. When personal.tex is absent, cv-llt.tex falls back to this file and
% omits the corresponding header fields entirely -- no placeholder text is
% typeset, so the CV still compiles cleanly from a fresh clone.

\newcommand{\cvphone}{}   % e.g. +86 123-4567-8910
\newcommand{\cvwechat}{}  % e.g. your\_wechat\_id  (escape _ as \_)
