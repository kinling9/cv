%!TEX encoding = UTF8
%!TEX root = cv-llt.tex
%
% Template for local settings.
%
% Copy this file to personal.tex and fill in your own values:
%
%     cp personal-example.tex personal.tex
%
% personal.tex is gitignored. When it is absent cv-llt.tex falls back to this
% file and leaves the corresponding header fields out entirely -- no
% placeholder text is typeset, so the CV still compiles from a fresh clone.

\newcommand{\cvphone}{}
\newcommand{\cvwechat}{}
